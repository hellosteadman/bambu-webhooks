Views and templates
===================

Bambu Webhooks provides a single view, which allows the user to assign a URL to each webhook.

.. automodule:: bambu_webhooks.views
   :members:

Templates
---------

``webhooks/manage.html``
~~~~~~~~~~~~~~~~~~~~~~~~

This template renders the management form, and is based on ``webhooks/base.html``, which in turn simply
extends ``base.html``.